{
  "name": "server",
  "version": "1.0.0",
  "description": "portfolyo server",
  "main": "server.js",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev:client": "cd ../client && npm start",
    "dev:server": "cd ../client && npm build && cd .. && npm start"
  },
  "_comments": {
    "dev:client": "start the app in development mode",
    "dev: server": "create a production build, then have the express server serve the app"
  },
  "author": "",
  "license": "ISC"
}